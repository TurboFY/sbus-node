info:
  name: tests-common
  version: "1.0"
  category: tests

include:
  - file: /etc/serve/include.d/library.yml

build:
  - node:
      node: npm ci && npm run lint && NPM_BUILD_VERSION={{ info.build-version }} npm run release --registry http://3.20.208.47:4873
      image: node:16
